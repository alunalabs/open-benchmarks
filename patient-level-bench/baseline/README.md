# Patient-Level Baselines

This folder contains compact patient-level baseline and control artifacts.

The current baseline folder is `crc_prior_controls_20260525/`, which contains
hard-donor and gene-shuffle controls for the CRC prior-control audit. These are
specificity checks, not promoted patient response predictors.
