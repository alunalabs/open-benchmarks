# BioBench Row Artifacts

This folder contains public BioBench row-definition artifacts.

- `legacy_v2/`: older production BioBench v2 ligand-module row set.

The v2 artifacts are included because they are useful for comparing older
ligand-receptor pathway rows against the newer BioBench surfaces described in
the top-level README.
